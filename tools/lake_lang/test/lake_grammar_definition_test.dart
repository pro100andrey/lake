// ignore_for_file: lines_longer_than_80_chars

// import 'package:lake_lang/lake_lang.dart';
// import 'package:petitparser/petitparser.dart';
import 'package:test/test.dart';

void main() {
  group('Lake grammar:', () {
    // final grammar = LakeGrammarDefinition();

    group('Import', () {
      // final parser = resolve(grammar.import());

      // test('single quote success', () {
      //   const input = "import 'base.lake'";
      //   final result = parser.parse(input);

      //   expect(result, isA<Success>());
      // });

      // test('double quote success', () {
      //   const input = 'import "base.lake"';
      //   final result = parser.parse(input);

      //   expect(result, isA<Success>());
      // });

      // test('invalid keyword', () {
      //   const input = 'impor "base.lake"';
      //   final result = parser.parse(input);

      //   expect(result, isA<Failure>());
      //   expect(result.message, equals('"import" expected'));
      // });

      // test('missing literal', () {
      //   const input = 'import';
      //   final result = parser.parse(input);

      //   expect(result, isA<Failure>());
      //   expect(result.message, equals('"\\\'" expected'));
      // });
    });
  });
}
